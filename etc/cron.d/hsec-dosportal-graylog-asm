#
# Managed by Ansible
#
# get IPs from graylog & insert them into dosportal
*/5 * * * * root /localservices/sbin/lockrun /var/run/dosportal-graylog-asm.lock /localservices/sbin/dosportal-graylog-asm.pl > /dev/null 2>&1

